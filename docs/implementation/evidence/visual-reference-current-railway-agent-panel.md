Reference image context: Railway project canvas with a narrow right-side `New Agent` panel opened. The important qualities to carry into Celerates Agent exploration are: persistent-but-unobtrusive entry point; narrow side panel overlay; contextual prompt/suggestion affordances; visible agent state without navigating away from the working page; cinematic/progressive motion is desired; this is interaction inspiration only, not a pixel clone or Railway-specific product requirement.